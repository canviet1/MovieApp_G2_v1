buildscript {
    repositories {
        google() // Include Google's Maven repository
        mavenCentral() // Include Maven Central
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.6.0") // Adjust the version as needed
    }
}
